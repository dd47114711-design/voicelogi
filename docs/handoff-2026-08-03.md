# 引き継ぎメモ（2026-08-03）

Next.js + Supabase への作り直しの、着手直前までの記録。
恒久的な開発ルールは `CLAUDE.md` にあるので、まずそちらを読むこと。
このファイルは「次に何をするか」だけを順番に書いたもの。完了したら消してよい。

## 今日やったこと

- 旧実装（`webapp/`）を読み、`CLAUDE.md` を新スタック向けに全面的に書き換えた
- スタックと開発フローをヒヤリングで確定した（下記）
- npm レジストリでバージョンを実測: Next.js 16.2.12 / React 19.2.8

コードは1行も書いていない。`CLAUDE.md` はまだコミットしていない。

## 確定した決定事項

ヒヤリング済み。蒸し返さないこと。

| 項目 | 決定 |
| --- | --- |
| 配置 | リポジトリ直下に新規作成。`webapp/` `scripts/` `output/` は `legacy/` へ退避 |
| スコープ | 最初から Supabase 込みで動く実装。機能は issue 単位で段階的に積む |
| 同期 | Supabase Realtime で複数端末に即時反映する |
| 認証 | Supabase Auth + RLS |
| DB アクセス | Drizzle ORM（スキーマ・マイグレーションをコード管理）+ supabase-js |
| テスト | Vitest（単体）+ Playwright（E2E）、TDD |
| issue 管理 | GitHub Issues を `gh` CLI で操作 |
| パッケージマネージャ | pnpm |
| ブランチ | `feature/<issue番号>-<説明>` → `dev` → `main` |

## 次にやること（この順番で）

### 0. 環境の準備 ※ブロッカー

この端末に Node.js も gh CLI も入っていない。これが無いと何も始まらない。
インストールは対話が入るので、ユーザー自身に `!` プレフィックスで実行してもらう。

```
! winget install OpenJS.NodeJS.LTS
! winget install GitHub.cli
```

その後:

```
! corepack enable pnpm
! gh auth login
```

確認: `node -v` / `pnpm -v` / `gh auth status` が通ること。

### 1. `CLAUDE.md` をコミット

現在 untracked。ユーザーに確認のうえコミットする。
`main` 直下での作業になるため、ブランチを切るかどうかも合わせて確認すること。

### 2. `dev` ブランチを作る

`main` から切る。まだ存在しない。

### 3. 最初の issue を立てる

以降はすべて issue 起点。想定している最初の数件:

1. 旧実装を `legacy/` へ退避する
2. Next.js 16 + TypeScript + Tailwind + shadcn/ui の初期セットアップ
3. Supabase プロジェクト作成、Drizzle スキーマ定義、マイグレーション
4. Vitest / Playwright のセットアップ
5. Vercel 連携（`main` = Production、`dev` = Preview）

各 issue は worktree を切って `superpowers:brainstorming` から始める。

## まだ決めていないこと

着手時に判断が要る。勝手に決めず確認する。

- **要件の移植範囲と優先順位。** 旧実装は配車ウィザード・出退勤 CSV・従業員/現場/車両/スケジュール管理まである。どれから作るか未定。
- **認証の適用範囲。** 「盤面の閲覧と出退勤打刻は無認証、管理機能だけ認証」という分け方も可能だと提示済みだが、結論は出ていない。Supabase Auth 採用だけが決まっている状態。
- **Supabase プロジェクトの実体。** まだ作られていない。接続情報も無い。
- **データ移行。** 旧実装のデータは各端末の localStorage にある。本番運用中のデータを引き継ぐ必要があるか未確認。

## 注意点

- `webapp/README.md` は実運用で確定した仕様が詳細に書かれた**資産**。作り直しで最も失われやすいので、退避しても捨てないこと。
- 引き継ぐべきドメインの不変条件（配置枠、通常ダンプと当日ダンプの分離、打刻の追記のみ運用ほか）は `CLAUDE.md` に転記済み。
- Next.js 16 は `params` / `searchParams` が Promise。14/15 時代の書き方を記憶で書かないこと。
